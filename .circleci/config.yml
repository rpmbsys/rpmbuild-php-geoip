version: 2.1

orbs:
  docker-rpmbuild: aursu/rpmbuild@1.1.8

parameters:
  artifactory-url:
    description: Artifactory URL
    type: string
    default: https://rpmb.jfrog.io

workflows:
  geoipbuild:
    jobs:
      - docker-rpmbuild/build:
          name: centos7geoip
          build_service: centos7geoip
          bintray-repo: php74custom
          bintray_uploader: centos7bintray
      - docker-rpmbuild/build:
          name: stream8geoip
          build_service: stream8geoip
          bintray-repo: php74custom
          bintray_uploader: stream8bintray
      - docker-rpmbuild/build:
          name: centos7geoipphp81
          build_file: docker-compose.php81.yml
          build_service: centos7geoipphp81
          bintray-repo: php81custom
          bintray_uploader: centos7bintray
      - docker-rpmbuild/build:
          name: stream8geoipphp81
          build_file: docker-compose.php81.yml
          build_service: stream8geoipphp81
          bintray-repo: php81custom
          bintray_uploader: stream8bintray